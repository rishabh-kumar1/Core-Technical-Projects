#!/bin/bash
#
# search-engine install
#
#
# Stop on errors, print commands
set -Eeuo pipefail
set -x

python3 -m venv env
source env/bin/activate
pip install -r requirements.txt

pip install -e index_server
pip install -e search_server